# 코인원
## 2022-08-23

```txt

[고객환경]
1. DB/Data Warehouse: MySQL, PostgeSQL, SQL Server, Redshift, BigQuery
2. DAC: DBSAFER
3. SAC: HIWARE
4. Observability Platform: splunk

[고객니즈 및 Pain point]
1. 클라우드에서 운용하는 DB에 대한 접근제어에 어려움이 있다 (ex. BigQuery)
2. DAC 에이전트 이슈가 있다.
3. PoC 진행 여부는 내부 논의후 결정하겠다.

[고객질문]
1. 사용자별로 접속 가능한 IP를 지정할 수 있는가?
- DB별로 IP대역대 지정 가능
- 야놀자의 경우는 중요DB는 VDI(AWS Workspace)를 통해 특정IP 대역대에서만 접근 가능

2. 대량 데이터 조회에 대한 권한 요청이 가능한가?
- 개발예정 (데이터 조회 임계치 초과시 사용자에게 팝업창 띄움 > 권한요청 > 권한 취득후 조회)

3. 대량 데이터 조회를 특정 사용자만 권한을 줄 수 없는가?
- 백로그에 있음 

4. DB세션은 계속 유지하고 있는가?
- 15분동안 사용자가 아무 반응이 없으면 세션을 끊음 

5. 로그에 대한 마스킹은 지원하는가?
- WHERE절은 개인정보는 아직 마스킹되지 않음 
- Syslog 형태로 splunk에 저장 가능 (syslog 정의서 참조)

6. 클라우드DB 연동 시 DB계정관리를 어떻게 하는가?
- 두나무의 경우 개발자마다 계정 생성, 계정정보를 공란으로 두면 각자 본인 계정으로 들어가면 됨 
- 쿼리파이API를 통해 계정을 한꺼번에 생성 가능

7. 시스템 접근제어 솔루션은 언제부터 사용할 수 있는가?
- 11월에 사용 가능 예상 
- 현재 쿼리파이와 하나의 솔루션으로 통합되고 라이선스에 따라 활성화/비활성화됨

```